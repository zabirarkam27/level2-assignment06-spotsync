services:
  - type: web
    name: spotsync-api
    runtime: go
    rootDir: spotsync-api
    buildCommand: go build -o main .
    startCommand: ./main
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: FRONTEND_URL
        sync: false
